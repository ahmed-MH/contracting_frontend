export const plannedSupervisorMonitoringSignals = [
    'Stripe webhook events',
    'Tenant lifecycle events',
    'Plan changes',
    'Supervisor actions',
    'Failed entitlement checks',
] as const;
